apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: micro-sword
  namespace: icb
  labels:
    app: micro-sword
spec:
  replicas: 1
  selector:
    matchLabels:
      app: micro-sword
  template:
    metadata:
      labels:
        app: micro-sword
    spec:
      containers:
      - name: micro-sword
        image: 10.10.10.3:5000/web/micro-sword:3.0
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        volumeMounts:
        - mountPath: /publish/appsettings.json
          name: web-config
          subPath: micro-sword.json
        env:
          - name: ACB_MODE
            value: Test

      volumes:
      - name: web-config
        configMap:
          name: web-config
          items: 
          - key: micro-sword.json
            path: micro-sword.json
        
