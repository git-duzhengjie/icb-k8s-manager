apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: micro-user
  namespace: icb
  labels:
    app: micro-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: micro-user
  template:
    metadata:
      labels:
        app: micro-user
    spec:
      containers:
      - name: micro-user
        image: 10.10.10.3:5000/web/micro-user:3.0
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        volumeMounts:
        - mountPath: /publish/appsettings.json
          name: web-config
          subPath: micro-user.json
        env:
          - name: ACB_MODE
            value: Test

      volumes:
      - name: web-config
        configMap:
          name: web-config
          items: 
          - key: micro-user.json
            path: micro-user.json
        
