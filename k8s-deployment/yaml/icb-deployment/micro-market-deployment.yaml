apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: micro-market
  namespace: icb
  labels:
    app: micro-market
spec:
  replicas: 1
  selector:
    matchLabels:
      app: micro-market
  template:
    metadata:
      labels:
        app: micro-market
    spec:
      containers:
      - name: micro-market
        image: 10.10.10.3:5000/web/micro-market:3.0
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        volumeMounts:
        - mountPath: /publish/appsettings.json
          name: web-config
          subPath: micro-market.json
        env:
          - name: ACB_MODE
            value: Test

      volumes:
      - name: web-config
        configMap:
          name: web-config
          items: 
          - key: micro-market.json
            path: micro-market.json
        
