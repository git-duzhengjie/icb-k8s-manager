apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: micro-main
  namespace: icb
  labels:
    app: micro-main
spec:
  replicas: 1
  selector:
    matchLabels:
      app: micro-main
  template:
    metadata:
      labels:
        app: micro-main
    spec:
      containers:
      - name: micro-main
        image: 10.10.10.3:5000/web/micro-main:3.0
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        volumeMounts:
        - mountPath: /publish/appsettings.json
          name: web-config
          subPath: micro-main.json
        env:
          - name: ACB_MODE
            value: Test

      volumes:
      - name: web-config
        configMap:
          name: web-config
          items: 
          - key: micro-main.json
            path: micro-main.json 
