kind: Service
apiVersion: v1
metadata:
  labels:
    k8s-app: micro-main
  name: micro-main
  namespace: icb
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
  selector:
    k8s-app: micro-main